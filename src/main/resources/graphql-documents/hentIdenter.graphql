query($ident: ID!) {
    hentIdenter(
        ident: $ident,
        grupper: [FOLKEREGISTERIDENT, AKTORID, NPID],
        historikk: true
    ) {
        identer {
            ident
            historisk
            gruppe
        }
    }
}
